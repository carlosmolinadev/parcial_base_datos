CREATE FUNCTION MOSTRAR_FAMILIAR (@COD_USUA int)
RETURNS TABLE
AS
RETURN
(
    SELECT [COD_FAMI]
      ,[COD_USUA]
      ,[NOM_FAMI]
      ,[APEPATER_FAMI]
      ,[APEMATER_FAMI]
      ,[GRADO_PARENTES_FAMI]
  FROM [Comercial].[FAMILIARES]
  WHERE COD_USUA = @COD_USUA
);
GO
